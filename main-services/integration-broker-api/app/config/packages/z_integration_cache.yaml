framework:
    cache:
        default_redis_provider: '%env(REDIS_URL)%'
        pools:
            cache.broker_token:
                adapter: cache.adapter.redis
            cache.scheduler_order_polling:
                adapter: cache.adapter.redis
